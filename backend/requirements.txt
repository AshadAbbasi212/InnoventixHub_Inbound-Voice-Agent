# Core Pipecat framework, pinned to what this code was verified against.
# "runner" extra is required for pipecat.runner.run.main() (the built-in dev server).
pipecat-ai[anthropic,openai,cartesia,silero,websocket,runner]==1.8.1

python-dotenv>=1.0.1
aiohttp>=3.9.0
loguru>=0.7.2

# Google auth (only needed if you switch back to the parked google_sheets.py/
# google_calendar.py scheduling attempt — not used by the active Calendly path).
google-auth>=2.30.0

# Only needed for test_console_chat.py
openai>=1.30.0
anthropic>=0.40.0
